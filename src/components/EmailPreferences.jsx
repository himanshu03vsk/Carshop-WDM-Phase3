const EmailPreferences = () => (
    <div className="section">
      <h3>Email Preferences</h3>
      {/* Checkboxes or toggles for email types */}
      <p>Email notification preferences will go here.</p>
    </div>
  );

export default EmailPreferences;